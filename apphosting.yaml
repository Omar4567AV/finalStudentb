# Firebase App Hosting CI/CD Configuration Layer
backend:
  runtime: nodejs20

env:
  - variable: GMAIL_USER
    value: ohalabi69@gmail.com
  
  # This tells the cloud runner to securely inject your secret password during building phases
  - variable: GMAIL_APP_PASS
    secret: gmail_app_password_secret
